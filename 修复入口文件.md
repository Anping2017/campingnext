# 修复 "app entry not found" 错误

## 问题原因

Expo Router 6 使用 `expo-router/entry` 作为入口点，不需要单独的 `index.js` 文件。

## 已完成的修复

1. ✅ **删除了 index.js 文件** - Expo Router 6 不需要它
2. ✅ **package.json 中的 main 字段** - 设置为 `expo-router/entry`
3. ✅ **确保 app 目录结构正确** - `app/_layout.tsx` 和 `app/index.tsx` 存在

## 正确的项目结构

```
apps/mobile/
├── app/
│   ├── _layout.tsx    # 根布局（必需）
│   └── index.tsx      # 首页（必需）
├── package.json        # main: "expo-router/entry"
└── app.json           # Expo 配置
```

## 验证配置

确保以下文件存在且配置正确：

### 1. package.json
```json
{
  "main": "expo-router/entry"
}
```

### 2. app/_layout.tsx
```typescript
import { Stack } from 'expo-router';

export default function RootLayout() {
  return (
    <Stack>
      <Stack.Screen name="index" />
    </Stack>
  );
}
```

### 3. app/index.tsx
```typescript
// 你的首页组件
```

## 如果还有问题

### 清理缓存并重启

```bash
cd apps/mobile
npx expo start --clear
```

### 检查依赖

```bash
cd apps/mobile
npx expo-doctor
```

### 重新安装依赖

```bash
# 从根目录
pnpm install

# 修复 Expo 依赖
cd apps/mobile
npx expo install --fix
```

## 常见错误

### 错误 1: "Cannot find module 'expo-router/entry'"

**解决**: 确保 expo-router 已安装
```bash
cd apps/mobile
npx expo install expo-router
```

### 错误 2: "app/_layout.tsx not found"

**解决**: 确保 `app/_layout.tsx` 文件存在

### 错误 3: "app/index.tsx not found"

**解决**: 确保 `app/index.tsx` 文件存在

## 启动项目

现在应该可以正常启动了：

```bash
pnpm dev:mobile
```

或者：

```bash
cd apps/mobile
pnpm start
```


