# 🚀 快速开始指南

## 第一步：安装依赖

### 1. 安装 pnpm（如果还没有）

```bash
# 使用 npm 安装
npm install -g pnpm

# 或使用其他方式
# https://pnpm.io/installation
```

### 2. 安装项目依赖

```bash
# 在项目根目录执行
pnpm install
```

这将会安装所有子项目的依赖（Web、移动端、共享包）。

## 第二步：启动开发服务器

### Web 端开发

```bash
# 启动 Next.js 开发服务器
pnpm dev:web

# 浏览器访问 http://localhost:3000
```

### 移动端开发

#### 方式 1: 使用 Expo Go（推荐，最简单）

1. **在手机上安装 Expo Go**
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. **启动开发服务器**
   ```bash
   pnpm dev:mobile
   ```

3. **扫描二维码**
   - 终端会显示一个二维码
   - 用 Expo Go App 扫描即可在手机上运行

#### 方式 2: 使用模拟器

**iOS 模拟器（需要 macOS + Xcode）:**
```bash
pnpm dev:mobile
# 然后按 i 键
```

**Android 模拟器（需要 Android Studio）:**
```bash
pnpm dev:mobile
# 然后按 a 键
```

## 第三步：验证安装

### 检查 Web 端

1. 访问 http://localhost:3000
2. 应该看到"露营个性化推荐应用"首页
3. 看到四个功能卡片

### 检查移动端

1. 在 Expo Go 中打开应用
2. 应该看到相同的首页内容
3. 可以滑动查看

## 常见问题解决

### ❌ 依赖安装失败

**问题：** `pnpm install` 报错

**解决：**
```bash
# 清理缓存
pnpm store prune

# 删除 node_modules 重新安装
rm -rf node_modules
pnpm install
```

### ❌ Web 端无法启动

**问题：** `pnpm dev:web` 报错

**解决：**
```bash
# 检查 Node.js 版本（需要 >= 18）
node -v

# 清理 Next.js 缓存
cd apps/web
rm -rf .next
pnpm dev
```

### ❌ 移动端无法连接

**问题：** Expo Go 无法连接到开发服务器

**解决：**
1. 确保手机和电脑在同一 WiFi 网络
2. 尝试使用 tunnel 模式：
   ```bash
   cd apps/mobile
   npx expo start --tunnel
   ```
3. 检查防火墙设置

### ❌ TypeScript 错误

**问题：** 类型检查报错

**解决：**
```bash
# 重新构建类型
pnpm type-check

# 如果还有问题，清理并重新安装
pnpm clean
pnpm install
```

### ❌ 模块找不到

**问题：** `Cannot find module 'common'`

**解决：**
```bash
# 确保在根目录执行安装
pnpm install

# 检查 workspace 配置
cat pnpm-workspace.yaml
```

## 开发工作流

### 日常开发流程

1. **启动 Web 端**
   ```bash
   pnpm dev:web
   ```

2. **在另一个终端启动移动端（如需要）**
   ```bash
   pnpm dev:mobile
   ```

3. **开始编码**
   - 修改代码
   - 保存文件
   - 查看浏览器/手机上的变化

### 使用 Cursor AI 开发

**提示词示例：**

```
在 Web 端创建一个营地搜索页面，包含搜索框和结果列表
使用 common 包中的 getCamps API
```

```
将 Web 端的营地列表组件迁移到移动端
使用 React Native 的 FlatList 组件
```

## 下一步

- 📖 阅读 [README.md](./README.md) 了解项目结构
- 📚 查看 [开发指南.md](./开发指南.md) 学习开发流程
- 🎯 开始开发你的第一个功能！

## 需要帮助？

- 查看项目文档
- 检查 [常见问题](#常见问题解决)
- 查看各框架官方文档



