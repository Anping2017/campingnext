# 🔒 API Key 安全提示

## ⚠️ 重要安全事项

### 1. 已配置的 API Key
- OpenAI API Key 已配置到 `apps/web/.env.local`
- 该文件已在 `.gitignore` 中，不会被提交到 Git

### 2. 安全建议

**✅ 已做：**
- `.env.local` 文件已添加到 `.gitignore`
- API Key 不会提交到版本控制

**⚠️ 注意事项：**
1. **不要**将 API Key 分享给他人
2. **不要**在代码中硬编码 API Key
3. **不要**将 API Key 提交到公开仓库
4. 如果 API Key 泄露，立即在 OpenAI 平台重新生成

### 3. 如果 API Key 泄露

如果发现 API Key 可能泄露：
1. 登录 OpenAI 平台：https://platform.openai.com/api-keys
2. 删除或重新生成 API Key
3. 更新 `.env.local` 文件中的新 Key

### 4. 环境变量使用

在代码中使用 API Key：
```typescript
// ✅ 正确：从环境变量读取
const apiKey = process.env.OPENAI_API_KEY;

// ❌ 错误：硬编码
const apiKey = 'sk-proj-...';
```

### 5. 部署到生产环境

部署到 Vercel 或其他平台时：
1. 在平台的环境变量设置中添加 `OPENAI_API_KEY`
2. **不要**在代码中硬编码
3. 使用平台的环境变量管理功能

## 📝 当前配置

- 开发环境：`apps/web/.env.local` ✅
- 生产环境：需要在部署平台配置


