# 修复说明

## 已修复的问题

### 1. ✅ Expo SDK 版本升级
- 将 Expo 从 SDK 50 升级到 SDK 54
- 更新了所有相关依赖版本：
  - `expo`: ~54.0.0
  - `expo-router`: ~4.0.0
  - `react-native`: 0.76.5
  - `react`: 18.3.1

### 2. ✅ 创建缺失的 Assets 文件
- 创建了占位文件（需要后续替换为实际图片）：
  - `assets/icon.png`
  - `assets/splash.png`
  - `assets/adaptive-icon.png`
  - `assets/favicon.png`

### 3. ✅ 修复依赖问题
- 从根目录移除了 expo 依赖（应该在 mobile 目录）
- 统一了 React 版本到 18.3.1

## 下一步操作

### 1. 清理并重新安装依赖

```bash
# 删除所有 node_modules
pnpm clean

# 重新安装依赖
pnpm install
```

### 2. 在 mobile 目录安装 Expo 兼容依赖

```bash
cd apps/mobile
npx expo install --fix
```

这会自动安装与 SDK 54 兼容的所有依赖版本。

### 3. 替换 Assets 文件

你需要创建实际的图片文件替换占位文件：

- **icon.png**: 1024x1024 的应用图标
- **splash.png**: 启动画面图片
- **adaptive-icon.png**: Android 自适应图标
- **favicon.png**: Web 端图标

或者暂时使用 Expo 的默认图标：

```bash
cd apps/mobile
npx expo install expo-asset
```

### 4. 启动项目

```bash
# 从根目录启动
pnpm dev:mobile

# 或从 mobile 目录启动
cd apps/mobile
pnpm start
```

## 如果还有 metro-cache 错误

如果仍然遇到 `metro-cache` 错误，尝试：

```bash
# 清理 Metro 缓存
cd apps/mobile
npx expo start --clear

# 或者删除缓存目录
rm -rf node_modules/.cache
rm -rf .expo
```

## 验证安装

运行 expo-doctor 检查：

```bash
cd apps/mobile
npx expo-doctor
```

应该显示所有检查通过。






