# 开发指南

## 🎯 开发流程

### 1. 新功能开发流程

```
Web 端开发 → 测试验证 → 提取共享代码 → 移动端迁移
```

**示例：添加营地搜索功能**

#### 步骤 1: Web 端开发

在 `apps/web/src/app/camps/page.tsx` 创建页面：

```typescript
'use client';

import { useState } from 'react';
import { getCamps, type Camp } from 'common';

export default function CampsPage() {
  const [camps, setCamps] = useState<Camp[]>([]);
  const [loading, setLoading] = useState(false);

  const handleSearch = async (location: string) => {
    setLoading(true);
    const result = await getCamps({ location });
    if (result.success && result.data) {
      setCamps(result.data);
    }
    setLoading(false);
  };

  return (
    <div>
      {/* UI 代码 */}
    </div>
  );
}
```

#### 步骤 2: 提取共享逻辑

如果 API 函数还不存在，在 `packages/common/src/api/camps.ts` 中添加：

```typescript
export async function searchCamps(query: string) {
  return get<Camp[]>(`/camps/search?q=${query}`);
}
```

#### 步骤 3: 移动端迁移

在 `apps/mobile/app/camps.tsx` 创建页面：

```typescript
import { useState } from 'react';
import { View, Text, FlatList } from 'react-native';
import { getCamps, type Camp } from 'common';

export default function CampsScreen() {
  const [camps, setCamps] = useState<Camp[]>([]);

  // 复用相同的 API 调用逻辑
  const handleSearch = async (location: string) => {
    const result = await getCamps({ location });
    if (result.success && result.data) {
      setCamps(result.data);
    }
  };

  return (
    <View>
      {/* React Native UI */}
    </View>
  );
}
```

### 2. 代码复用原则

**✅ 应该放在 `common` 包：**
- 类型定义（TypeScript interfaces/types）
- API 调用函数
- 业务逻辑函数
- 工具函数（格式化、计算等）
- 常量定义

**❌ 不应该放在 `common` 包：**
- UI 组件（Web 和移动端 UI 差异大）
- 样式代码
- 平台特定的代码

### 3. 状态管理

**推荐方案：Zustand（轻量级）**

在 `packages/common/src/store/` 创建共享状态：

```typescript
// packages/common/src/store/useCampsStore.ts
import { create } from 'zustand';
import type { Camp } from '../types';

interface CampsStore {
  camps: Camp[];
  setCamps: (camps: Camp[]) => void;
}

export const useCampsStore = create<CampsStore>((set) => ({
  camps: [],
  setCamps: (camps) => set({ camps }),
}));
```

**Web 端使用：**
```typescript
import { useCampsStore } from 'common';

export default function Page() {
  const { camps, setCamps } = useCampsStore();
  // ...
}
```

**移动端使用：**
```typescript
import { useCampsStore } from 'common';

export default function Screen() {
  const { camps, setCamps } = useCampsStore();
  // ...
}
```

## 🔄 工作流建议

### 日常开发

1. **先做 Web 端**
   - 快速验证功能
   - 使用浏览器 DevTools 调试
   - 测试所有边界情况

2. **代码审查**
   - 确保逻辑清晰
   - 提取可复用部分

3. **迁移到移动端**
   - 复用业务逻辑
   - 适配移动端 UI
   - 测试不同屏幕尺寸

### 使用 Cursor AI 开发

**提示词示例：**

```
在 Web 端创建一个营地详情页面，包含：
- 营地基本信息展示
- 图片轮播
- 设施列表
- 评价展示
- 使用 common 包中的类型和 API
```

```
将 Web 端的营地列表页面迁移到移动端，
复用 common 包中的 getCamps API 和 Camp 类型
```

## 📝 代码示例

### API 调用示例

```typescript
// 在 Web 或移动端都可以使用
import { getCamps, getCampById } from 'common';

// 获取营地列表
const result = await getCamps({
  location: '北京',
  minPrice: 100,
  maxPrice: 500,
});

if (result.success && result.data) {
  console.log(result.data);
}

// 获取单个营地
const campResult = await getCampById('camp-123');
```

### 工具函数示例

```typescript
import { formatDate, formatPrice, calculateDistance } from 'common';

// 格式化日期
const date = formatDate(new Date()); // "2024年1月1日"

// 格式化价格
const price = formatPrice(299); // "¥299"

// 计算距离
const distance = calculateDistance(39.9, 116.4, 40.0, 116.5); // 公里
```

## 🧪 测试建议

### Web 端测试

- 使用浏览器 DevTools
- Next.js 自带热重载
- 可以使用 Playwright 做 E2E 测试

### 移动端测试

- 使用 Expo Go 在真机测试
- 使用 iOS/Android 模拟器
- 测试不同屏幕尺寸

## 🐛 调试技巧

### Web 端

```typescript
// 使用 console.log（开发环境）
console.log('Debug info:', data);

// 使用 React DevTools
// 安装浏览器扩展
```

### 移动端

```typescript
// 使用 console.log（会在终端显示）
console.log('Debug info:', data);

// 使用 React Native Debugger
// 或 Expo DevTools
```

### 共享代码调试

```typescript
// 在 common 包中添加日志
export async function getCamps(params) {
  console.log('[API] getCamps called with:', params);
  // ...
}
```

## 📦 添加新依赖

### Web 端依赖

```bash
cd apps/web
pnpm add <package-name>
```

### 移动端依赖

```bash
cd apps/mobile
pnpm add <package-name>
```

### 共享依赖

```bash
cd packages/common
pnpm add <package-name>
```

## 🚀 性能优化

### Web 端

- 使用 Next.js 的 Image 组件
- 代码分割和懒加载
- 使用 React Query 缓存 API 数据

### 移动端

- 使用 FlatList 渲染长列表
- 图片懒加载
- 使用 React Query 缓存数据

### 共享优化

- API 响应缓存
- 防抖和节流（已在 utils 中提供）

## 📚 下一步

1. 阅读 [Next.js 文档](https://nextjs.org/docs)
2. 学习 [React Native 基础](https://reactnative.dev/docs/getting-started)
3. 熟悉 [Expo Router](https://docs.expo.dev/router/introduction/)
4. 了解 [TypeScript](https://www.typescriptlang.org/docs)






